import { TreeNode } from './TreeNode.js';
export declare class Root extends TreeNode {
    #private;
    constructor(name: string, path: string, typeNumber: number, treeId: unknown, depth: number);
}
