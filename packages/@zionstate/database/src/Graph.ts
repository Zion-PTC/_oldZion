export {};
enum GraphDBs {
  neo4j = "neo4j",
  dGraph = "dGraph",
}

enum UseCases {
  internal_graph_within_companies,
  fraud_detection_finance,
  racomendation_engines,
}

enum Pros {
  quicker_if_lot_of_joins,
}

enum Cons {}

enum Acronym {
  cypher,
}
