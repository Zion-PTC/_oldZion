import { runProcess } from "../../IPFS/lib/runProcess";

const packages = "packages";
const apps = "apps";

const main = function () {};

runProcess(main, { successMess: "Succesfully retrived all dependencies" });
