export {};

enum RealationalDBs {
  mySQL = "mySQL",
  postGres = "postGres",
  SQLServer = "SQLserver",
  cockroach = "CockRoach",
}

enum UseCases {
  banks,
  financial,
  most_apps,
}

enum Pros {
  ACID,
}

enum Cons {
  doesnt_scale,
}
