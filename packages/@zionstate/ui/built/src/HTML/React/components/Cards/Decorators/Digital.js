import React from "react";
export default function Digital(props) {
    const { Parent } = props;
    return (React.createElement(Parent, null,
        React.createElement("div", null, "Digital")));
}
