/**
 * converts an hsl tuple of numbers into an hex string
 *
 * usage
 *
 * ```js
 * hslToHex(0, 100, 50) // returns #ff0000
 * ```
 */
export declare function hslToHex(h: number, s: number, l: number): string;
