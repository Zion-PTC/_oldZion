const { expect } = require("chai");
const { ethers } = require("hardhat");

let TokenShop, tokenShop, Membership, membership, Zion, zion, USDC, usdc, DAO, deployer, buyer1, buyer2

beforeEach("Sending wrong token to contract",()=>{
  
})